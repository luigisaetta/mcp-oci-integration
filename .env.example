# Copy this file to `.env` and set real values.
# `.env` is gitignored.

VECTOR_DB_USER=replace_me
VECTOR_DB_PWD=replace_me
VECTOR_WALLET_PWD=replace_me
VECTOR_DSN=replace_me
VECTOR_WALLET_DIR_HOST=/absolute/path/to/your/wallet

# Citation PNG static server
CITATION_DIR_ROOT_HOST=/Users/lsaetta/Progetti/work-iren/pages
CITATION_SERVER_BIND_IP=0.0.0.0
CITATION_SERVER_PORT=8008
# Use localhost on local runs, and the VM public DNS/IP on Ubuntu deployments
CITATION_BASE_URL=http://127.0.0.1:8008/
